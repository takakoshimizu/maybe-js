{
  "name": "jsmaybe",
  "version": "0.4.0",
  "description": "A simple, useful, but somewhat incorrect implementation of the Maybe monad in Javascript.",
  "main": "./lib/index.js",
  "types": "./lib/index.d.ts",
  "repository": "git@github.com:takakoshimizu/maybe-js.git",
  "author": "Erich Dalton <c.halcyon.days@gmail.com>",
  "license": "MIT",
  "files": [
    "/lib",
    "/types"
  ],
  "scripts": {
    "build": "tsc src/index.ts --outDir lib --declaration",
    "test": "jest test/*.ts",
    "prepublish": "yarn build",
    "preversion": "npm test",
    "version": "git add .",
    "postversion": "git push && git push --tags"
  },
  "devDependencies": {
    "@types/jest": "^25.2.3",
    "jest": "^26.0.1",
    "ts-jest": "^26.1.0",
    "typescript": "^3.9.5"
  },
  "dependencies": {}
}
